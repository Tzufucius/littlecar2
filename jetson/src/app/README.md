# app 目录说明

本目录存放应用入口和视觉流程编排代码。

- `main.py`：主入口，通过文件顶部常量选择单张图片或摄像头循环。
- `robot_main.py`：视觉主流程，负责组织二维码识别、YOLO 推理和结果输出。
- `workflow.py`：轻量工作流封装，把视觉服务结果转换为视觉事件。

当前不接收命令行参数，也不读取外部配置文件。运行模式、模型、摄像头和测试图片路径都直接写在源码顶部常量中。
