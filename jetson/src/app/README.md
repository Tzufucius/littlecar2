# app 目录说明

本目录保存视觉主流程的入口和编排逻辑。

- `main.py`：程序入口，通过顶部常量选择单张图片或摄像头循环。
- `robot_main.py`：视觉主流程，负责二维码识别、YOLO 推理和结果输出。
- `workflow.py`：轻量工作流封装，把视觉服务结果转成事件。

这里不负责串口协议通信。Jetson 与 STM32 的底层协议封装在 `src/protocol/` 中，应用层需要联动底盘时，再显式组合 `Car`。

当前仍然不读取外部配置文件。运行模式、模型、摄像头和图片路径都写在源码顶部常量里。
