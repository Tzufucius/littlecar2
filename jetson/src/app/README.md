# app 目录说明

本目录存放应用入口和流程编排代码。

- `main.py`：命令行入口，通过文件顶部常量选择运行模式
- `robot_main.py`：整车主流程主函数位置，负责组织视觉识别、任务事件和 STM32 通信
- `workflow.py`：把视觉服务结果转换为后续任务可使用的事件

当前不使用外部配置文件。运行模式、模型、摄像头和串口参数直接写在 `main.py` 和 `robot_main.py` 顶部常量中。
